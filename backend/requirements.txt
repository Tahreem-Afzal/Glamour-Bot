fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
pydantic
pydantic[email]
python-multipart==0.0.9

# Try-on: garment catalog DB + image handling
sqlalchemy==2.0.30
Pillow==10.3.0
httpx==0.27.0
fashn

# Image generation (Stability AI)
requests

# Chatbot: knowledge base loading
python-docx
pypdf

# Chatbot: vector search — fastembed runs the same all-MiniLM-L6-v2 model
# via ONNX Runtime instead of sentence-transformers/torch. This fits
# comfortably in Render's 512MB free/starter RAM limit, unlike torch which
# was causing out-of-memory crashes on deploy.
fastembed
faiss-cpu
numpy

# Chatbot: LLM + voice
groq
gtts

# Auth: password hashing, JWT issue/verify, Google sign-in token verification
passlib[bcrypt]
bcrypt==4.0.1
python-jose[cryptography]
google-auth